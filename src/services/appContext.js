import { createContext } from "react";

export const Ingredients = createContext(null),
  Cart = createContext(null),
  Order = createContext(null),
  TotalCost = createContext(null),
  OrderNumber = createContext(null);
